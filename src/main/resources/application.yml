server:
  port: 8081

spring:
  datasource:
    password: qqqqqqqq
    url: jdbc:postgresql://localhost:5432/fornitore3
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true